const tableModels = '/tableList'

export const apiRoute = '/api';

export const query = 'query'

export const tablesRoute = `${apiRoute}${tableModels}`;

const upload = '/upload'

export const csvDataUpload = `${upload}/csvUpload`